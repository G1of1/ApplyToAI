services:
  - type: web
    name: applytoai
    runtime: python
    buildCommand: |
      cd frontend && npm install && npm run build
    startCommand: gunicorn app:app
    workingDir: backend 
    envVars:
      - key: PYTHON_VERSION
        value: 3.10

